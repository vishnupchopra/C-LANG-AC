#include<stdio.h>
#include<math.h>//this is a comment.
int main() {
    int number;
    scanf("%d",&number);
    if(number >= 1) {printf("given number is a natural no.");} else{printf("given number is not an natural number.");}
    return 0;
}